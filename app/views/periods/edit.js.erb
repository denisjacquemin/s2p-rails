$('#newCompetencyModal .modal-body').html("<%= escape_javascript(render 'form', period: @period) %>");
$('#newCompetencyModal #modalTitle').html("Mise à jour période");
$("#newCompetencyModal").modal('show');