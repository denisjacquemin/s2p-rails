$('#newEvaluationModal .modal-body').html("<%= escape_javascript(render 'form', evaluation: @evaluation, period_id: @period_id, competency_id: @competency_id) %>");
$('#newEvaluationModal #modalTitle').html("Nouvelle évaluation");
$('#newEvaluationModal .modal-body #datetimepickerdate').datetimepicker({
    locale: 'fr',
    format: 'DD/MM/YYYY'
  });

$("#newEvaluationModal").modal('show');