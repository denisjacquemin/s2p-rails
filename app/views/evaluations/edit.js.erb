$('#myModal .modal-body').html("<%= escape_javascript(render 'form', evaluation: @evaluation, period_id: @evaluation.period_id, competency_id: @evaluation.competency_id) %>");
$('#myModal #modalTitle').html("Mise à jour de l'évaluation");
$('#myModal .modal-body #datetimepickerdate').datetimepicker({
    locale: 'fr',
    format: 'DD/MM/YYYY'
  });
$("#myModal").modal('show');