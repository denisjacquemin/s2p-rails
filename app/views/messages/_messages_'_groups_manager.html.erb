<div class="row groups">
    <div class="col-xs-5">
      <div class="panel panel-default">
        <div class="panel-heading"><%= t('views.messages.listofstudents') %></div>
        <div class="panel-body table-responsive">
            <table class="table table-hover table-condensed" >
              <thead>
                <th><input type="checkbox" /></th>
                <th>Nom du groupe</th>
              </thead>
              <tbody id="group_list">
                <% Group.by_school(current_school).each do |group| %>
                  <tr>
                    <td><input type="checkbox" value="<%= group.id %>"/></td>
                    <td><label><%= group.name %></label></td>
                  </tr>
                <% end %>
              </tbody>
            </table>
        </div>
      </div>
    </div>
    <div class="col-xs-2 buttons">
      <button type="button" class="btn " id="add" aria-label="Left Align">
        Ajouter
        <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
      </button>
      <button type="button" class="btn" id="remove" aria-label="Left Align">
        <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
        Enlever
      </button>
    </div>
    <div class="col-xs-5">
        <div class="panel panel-default">
          <div class="panel-heading">Groupes du message</div>
          <div class="panel-body table-responsive">
            <table class="table table-hover table-condensed" id="in_groups">
              <thead>
                <th><input type="checkbox" /></th>
                <th>Nom du groupe</th>
              </thead>
              <tbody>
                <% message.groups_obj.each do |g| %>
                  <tr>
                    <%= fields_for g do |g_form| %>
                      <%= g_form.hidden_field :id, multiple: true%>
                      <td><input type="checkbox" id="group[<%= g.id %>]"/></td>
                      <td><%= g_form.label :id, g.name %></td>
                    <% end %>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div>
    </div>
</div>
