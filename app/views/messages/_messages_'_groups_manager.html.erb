<div class="row groups">
    <div class="col-xs-5">
      <div class="panel panel-default">
        <div class="panel-heading">
          <ul class="nav nav-pills">
            <li role="presentation" class="active"><a data-toggle="pill" href="#groups"><i class="fa fa-users"></i> Groupes</a></li>
            <li role="presentation"><a data-toggle="pill" href="#students"><i class="fa fa-user"></i> Elèves</a></li>
          </ul>
        </div>
        <div class="panel-body table-responsive">
          <div class="tab-content">
            <div role="tabpanel" class="tab-pane fade in active" id="groups">
              <% groups = Message.recipients_groups(current_school, current_user) %>
              <% if groups.present? %>
                <table class="table table-hover table-condensed" >
                  <tbody id="group_list">
                    <% groups.each do |group| %>
                      <tr>
                        <td><input type="checkbox" value="<%= group.id %>"/></td>
                        <td><label><%= group.name %></label></td>
                      </tr>
                    <% end %>
                  </tbody>
                </table>
              <% end %>
              <% if groups.empty? %>
                <p><%= I18n.t('views.messages.groups_no_rights', admins: current_school.admins.map {|u| u.fullname}.join(', ')) %></p>
              <% end %>
            </div>
            <div role="tabpanel" class="tab-pane fade in" id="students">
              <% students = Message.recipients_students(current_school, current_user) %>
              <% if students.present? %>
                <table class="table table-hover table-condensed" >
                  <tbody id="student_list">
                    <% students.each do |student| %>
                      <tr>
                        <td><input type="checkbox" value="<%= student.id %>"/></td>
                        <td><label><%= student.fullname %></label></td>
                        <td><%= student.level %></td>
                        <td><%= student.classroom %></td>
                      </tr>
                    <% end %>
                  </tbody>
                </table>
              <% end %>
              <% if students.empty? %>
                <p><%= I18n.t('views.messages.students_no_rights', admins: current_school.admins.map {|u| u.fullname}.join(', ')) %></p>
              <% end %>
            </div>
          </div>
        </div>
      </div>
    </div>
    <div class="col-xs-2 buttons">
      <button type="button" class="btn " id="add" aria-label="Left Align">
        Ajouter
        <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
      </button>
      <button type="button" class="btn" id="remove" aria-label="Left Align">
        <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
        Enlever
      </button>
    </div>
    <div class="col-xs-5">
        <div class="panel panel-default">
          <div class="panel-heading">Destinataires</div>
          <div class="panel-body table-responsive">
            <table class="table table-hover table-condensed" id="in_groups">
              <tbody>
                <% message.groups_obj.each do |g| %>
                  <tr>
                    <%= fields_for g do |g_form| %>
                      <%= g_form.hidden_field :id, multiple: true%>
                      <td><input type="checkbox" id="group[<%= g.id %>]"/></td>
                      <td><i class="fa fa-users"></i> <%= g_form.label :id, g.name %></td>
                    <% end %>
                  </tr>
                <% end %>
                <% message.students_obj.each do |s| %>
                  <tr>
                    <%= fields_for s do |s_form| %>
                      <%= s_form.hidden_field :id, multiple: true%>
                      <td><input type="checkbox" id="student[<%= s.id %>]"/></td>
                      <td><i class="fa fa-user"></i> <%= s_form.label :id, s.fullname %></td>
                    <% end %>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div>
    </div>
</div>
