<%= form_for(student) do |f| %>
  <%= render 'layouts/errors', object: student %>

  <%
    submit_label = t('views.students.new.submit')
    submit_label = t('views.students.edit.submit') unless @student.new_record?
  %>

  <div class="row">
    <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12 ">
      <div class="pull-right">
        <%= f.submit submit_label, class: 'btn btn-primary' %>
        <% if policy(student).destroy? and not student.new_record? %>
          <%= link_to t('views.global.destroy'), student, method: :delete, data: { confirm: t('views.global.areyousure') }, class: 'btn btn-danger' %>
        <% end %>
      </div>
    </div>
  </div>

  <div class="row">
    <div class="col-xs-12 col-sm-12 col-md-6 col-lg-6">
      <div class="form-group">
        <%= f.label :lastname %>
        <%= f.text_field :lastname, class: 'form-control' %>
      </div>
    </div>
    <div class="col-xs-12 col-sm-12 col-md-6 col-lg-6">
      <div class="form-group">
        <%= f.label :firstname %>
        <%= f.text_field :firstname, class: 'form-control' %>
      </div>
    </div>
  </div>

  <div class="row">
    <div class="col-xs-12 col-sm-12 col-md-6 col-lg-6">
      <div class="form-group">
        <%= f.label :level %>
        <%= f.text_field :level, class: 'form-control' %>
      </div>
    </div>
    <div class="col-xs-12 col-sm-12 col-md-6 col-lg-6">
      <div class="form-group">
        <%= f.label :classroom %>
        <%= f.text_field :classroom, class: 'form-control' %>
      </div>
    </div>
  </div>
  <div class="row">
    <div class="col-xs-12 col-sm-12 col-md-9 col-lg-9">
      <div class="form-group">
        <%= f.label :emails %>
        <%= f.text_field :emails, class: 'form-control' %>
        <span id="helpBlock" class="help-block">Un ou plusieurs emails séparés par un espace.</span>
      </div>
    </div>
    <div class="col-xs-12 col-sm-12 col-md-3 col-lg-3">
      <div class="form-group">
        <%= f.label :sent_message_by_email %><br/>
        <%= f.check_box :sent_message_by_email %>
      </div>
    </div>
  </div>
  <% unless student.new_record? %>
    <div class="row">
      <div class="col-xs-12 col-sm-12 col-md-6 col-lg-6">
        <div class="form-group">
          <%= f.label :code %>
          <%= f.text_field :code, class: 'form-control' %>
        </div>
      </div>
    </div>
  <% end %>

  <% if current_user.superadmin? %>
    <div class="row">
      <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
        <div class="form-group">
          <%= f.label :school %>
          <%= f.select(:school_id, School.all.map {|s| [s.name,s.id]}, {}, {class: "form-control"})  %>
        </div>
      </div>
    </div>
  <% end %>
  <div class="row" id="student_manage_group">
    <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
      <%= render "students_'_groups_manager", student: @student %>
    </div>
  </div>
<% end %>
