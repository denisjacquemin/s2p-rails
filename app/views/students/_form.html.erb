<%= form_for(student) do |f| %>
  <% if student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student.errors.count, "error") %> prohibited this student from being saved:</h2>

      <ul>
      <% student.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="col-xs-12 col-sm-12 col-md-6 col-lg-6">
      <div class="form-group">
        <%= f.label :lastname %>
        <%= f.text_field :lastname, class: 'form-control' %>
      </div>
    </div>
    <div class="col-xs-12 col-sm-12 col-md-6 col-lg-6">
      <div class="form-group">
        <%= f.label :firstname %>
        <%= f.text_field :firstname, class: 'form-control' %>
      </div>
    </div>
  </div>

  <div class="row">
    <div class="col-xs-12 col-sm-12 col-md-6 col-lg-6">
      <div class="form-group">
        <%= f.label :level %>
        <%= f.text_field :level, class: 'form-control' %>
      </div>
    </div>
    <div class="col-xs-12 col-sm-12 col-md-6 col-lg-6">
      <div class="form-group">
        <%= f.label :classroom %>
        <%= f.text_field :classroom, class: 'form-control' %>
      </div>
    </div>
  </div>
  <% unless student.new_record? %>
    <div class="row">
      <div class="col-xs-12 col-sm-12 col-md-6 col-lg-6">
        <div class="form-group">
          <%= f.label :code %>
          <%= f.text_field :code, class: 'form-control' %>
        </div>
      </div>
    </div>
  <% end %>

  <% if current_user.superadmin? %>
    <div class="form-group">
      <%= f.label :school %>
      <%= f.select(:school_id, School.all.map {|s| [s.name,s.id]}, {}, {class: "form-control"})  %>
    </div>
  <% end %>

  <%
    submit_label = t('views.students.new.submit')
    submit_label = t('views.students.edit.submit') unless @student.new_record?
  %>


  <%= f.submit submit_label, class: 'btn btn-primary' %>
  <% if policy(student).destroy? %>
    <td><%= link_to t('views.global.destroy'), student, method: :delete, data: { confirm: t('views.global.areyousure') }, class: 'btn btn-danger' %></td>
  <% end %>
<% end %>
