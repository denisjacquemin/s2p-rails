$('#newCompetencyModal .modal-body').html("<%= escape_javascript(render 'form', period: @rating_comment) %>");
$('#newCompetencyModal #modalTitle').html("Mise à jour commentaire");
$("#newCompetencyModal").modal('show');