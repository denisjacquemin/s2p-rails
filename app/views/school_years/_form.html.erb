<%= form_with(model: school_year, local: true) do |form| %>
  <% if school_year.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(school_year.errors.count, "error") %> prohibited this school_year from being saved:</h2>

      <ul>
      <% school_year.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :start_date %>
    <%= form.datetime_select :start_date %>
  </div>

  <div class="field">
    <%= form.label :end_date %>
    <%= form.datetime_select :end_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
