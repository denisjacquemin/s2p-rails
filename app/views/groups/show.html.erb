<p id="notice"><%= notice %></p>

<p>
  <strong>Nom:</strong>
  <%= @group.name %>
</p>
<p>
  <strong>Liste des membres:</strong>
  <ul>
    <% @group.students.each do |s| %>
      <li><%= s.fullname %></li>
    <% end %>
    <% @group.writers.active_and_invitation_accepted.each do |s| %>
      <li><%= s.fullname %></li>
    <% end %>
  </ul>
</p>
