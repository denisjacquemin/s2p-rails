$('#emptyCompetenciesMsg').empty();
<% if @current_competency.title_only %>
    $('#ratings_table').html("<tr><td>Pas de cotation pour cette matière</td></tr>")
<% else %>
    $('#ratings_table').html("<%= escape_javascript(render 'students', students: @students, competency_selected_id: @competency_selected_id) %>");
<% end %>