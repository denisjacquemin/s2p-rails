$('#ratings_table table').replaceWith("<%= escape_javascript(render 'ratings_by_student', competencies: @competencies, periods: @periods, student_ratings: @student_ratings) %>");
if ($('*[data-emoji-picker="true"]').length > 0) {
  console.log("if is true")
  try {
    new EmojiPicker();
  } catch (_error) {}
}