<% competencies.each do |c| %>
    <option <%= 'disabled' if c.title_only %> value="<%= c.id %>" <%= 'selected' if competency_selected_id == c.id.to_s %>>  <%= '--'  * ((c.level-1) *2) %> <%= c.name %></option>
<% end %>
